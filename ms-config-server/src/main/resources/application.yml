server:
  port: 7071
spring:
  application:
    name: ms-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JIMY-BETANZOS-1/NUEVO.git # ruta del repositorio git
          searchPaths: config-data
          default-label: break
  security:
    user:
      name: root
      password: yimi